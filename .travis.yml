language: java
jdk:
  - oraclejdk8

before_script:
  - git clone $TEST_PACKAGE tests
  - mkdir test
  - git clone $TEST
  - ./setup.sh

script:
  - ./gradlew clean
  - ./gradlew build

notifications:
  webhooks:
    urls:
      - http://non.com/travisci
